specVersion: "v1beta"
serviceId: "newemr"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "newemr-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
